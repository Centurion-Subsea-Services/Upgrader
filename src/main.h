#pragma once
#include "DriverCore/log/logger.h"

/**
 * Application entry point
 * @param argc the count of the arguments
 * @param argv the pointer to a list of argument strings
 */
int main(int argc, char **argv);
